- Para este projeto (Promptdown-aula, na raiz, não dentro de doc):

```bash
cd "c:\projetos\repositorys-root\Promptdown-aula"
node scripts/serve.mjs
```

- Isso sobe um servidor estático em http://localhost:5500 servindo a pasta `doc/`
  (onde mora `index.html`, espelhando a estrutura publicada no GitHub Pages).
- Depois abra http://localhost:5500/ no navegador.
