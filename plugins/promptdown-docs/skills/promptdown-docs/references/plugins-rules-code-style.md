`plugins/promptdown-helpers/rules/code-style.md` é a fonte canônica da rule
de estilo — escrita aqui uma vez, gerada para todos os harnesses pelo Makefile.

## Como distribuir após editar

```bash
make generate-all
```

Sincroniza para `.claude/rules/`, `.cursor/rules/` e `.agents/rules/`.

## Conteúdo esperado

Ver `claude-rules-code-style.md`.

## Fontes

- `plugins.md`
- `makefile.md`
