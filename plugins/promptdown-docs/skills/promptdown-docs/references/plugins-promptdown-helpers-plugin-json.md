`plugins/promptdown-helpers/plugin.json` é o marker obrigatório que identifica
a pasta como um plugin agnóstico de harness.

## Conteúdo

```json
{ "name": "promptdown-helpers" }
```

O campo `name` é opcional — se ausente, o default é o nome da pasta.

## Fontes

- `plugins.md`
